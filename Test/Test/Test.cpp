// Test.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include "pch.h"
#include <iostream>

int main()
{
	;
	int a = 1, b = 2, c = 3;
	a = b = c;
	2 + 3;
	a + b;
	if (a = 1)
	{

	}
	a = --b;
	a = b--;
	--a = 3;
	//a-- = 3;
	//2 = 3;
	switch (a)
	{
	case 0:
		switch (b)
		{
		default:
			switch (c)
			{
			default:
				break;
			}
			break;
		}
	default:
		if (a)
			;
		break;
	}
	;;;
	while (1) ;
	while (a) {}

	for (a; a; a);
	for (;;);

	std::cout << "a = " << a << ", b = " << b << ", c = " << c;
}
